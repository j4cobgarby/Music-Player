var rewindBtn,
    pauseplayBtn,
    forwardBtn,
    volSlider,
    volThumb;

var volValue;

function initPlayback() {
    rewindBtn = document.getElementById('rewind');
    pauseplayBtn = document.getElementById('pause');
    forwardBtn = document.getElementById('forward');

    volSlider = document.getElementById('volSlider');
    volThumb = document.getElementById('volThumb');
}
